// Copy to Config/Signing.xcconfig (ignored by git) and set your own team ID.
// A stable Apple Development identity keeps the Screen Recording grant across rebuilds.
CODE_SIGN_IDENTITY = Apple Development
DEVELOPMENT_TEAM = YOURTEAMID
