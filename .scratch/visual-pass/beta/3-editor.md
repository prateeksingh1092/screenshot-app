# Beta person 3: editor person

Run: 2026-09-24, about 19:50–20:05 local, driven directly from the main Claude session (not detached), with Frisket 73be4ce (PID 47669). Prateek allowed any screen. Captures were taken on the ASUS (scale 1). Input was CGEvents to the HID tap plus AX reads and presses. Pixel checks were run on the clipboard PNG and the History file.

| Surface | What I did | What I saw | What VoiceOver would hear (AX) | What failed or felt unfinished | Evidence |
| --- | --- | --- | --- | --- | --- |
| Open editor with AXPress "Edit capture" | Card Edit through AX, with the pattern app frontmost | The editor opened **behind** the pattern app's full-display window, and Frisket was not activated | Window "Edit Capture", focused=1 inside Frisket | A real click on Edit (below) does activate. Only AX or keyboard paths hit this | — |
| Open editor with a real click on Edit | Clicked the card's pencil | Frisket became frontmost and the editor opened on the capture's display, in front | Canvas: "Capture canvas. Drag to hide pixels with a solid black redaction." Hint text is an AXStaticText | — | — |
| Toolbar | Read at the default width (760 pt for 320- and 400-wide captures) | Tools, Label field and Undo are visible. **Close, Copy, Save and Done are all hidden in the `>>` overflow menu** | Checkboxes "Solid Redaction", "Crop", "Arrow", "Shape", "Text", "Blur", "Magnify"; "Annotation label text"; "Undo"; "more toolbar items" | **High (UX): the editor's only exits are hidden at its own default size.** A new user sees no Done or Copy | `evidence/p3-toolbar-overflow.png` |
| **A. Tall capture, arrow and label** | 400×500 capture; arrow at image row 450; label near row 40; Copy from overflow | **The preview shows one label and the arrow. The copied and History PNG shows the label twice (rows 40 and 296) and no arrow.** Signal-red ink: rows 0–255 = 731 px, rows 256–499 = 731 px, arrow band (rows 430–470) = 0 | — | **Critical: saved edits differ from what the user saw.** The History file is byte-identical to the clipboard PNG, so the broken image is what is kept. Cause is `DocumentRenderer.swift:305-314` (arrow and text ignore `rowShift`) | `evidence/p3-A-editor-preview.png`, `p3-A-copied-output.png` |
| **B. Label text** | Typed `v2.1 $4.99 -10%` | The field shows the string; **the canvas and output draw `V21 499 10`** | Field value "v2.1 $4.99 -10%" | **Medium–high: prices, versions and percentages are silently changed** | same PNGs |
| **C. Return in the label field** | Field focused and Frisket frontmost; pressed Return | Nothing happened. The editor stayed open and no History row was added | — | **Refuted.** Return does not finalize from the field | — |
| Solid redaction, blur and magnify | Redaction over the red quadrant, blur over the redaction's edge and green, magnify inside the redaction; Done | Output 320×180. `--verify-redacted` **PASS**: red quadrant exactly (0,0,0,255), no red pixel anywhere | Hints: "Drag a box. It is painted solid black and stays hidden under blur." / "…Magnify does not hide pixels. Use Solid Redaction to conceal." | — | `evidence/p3-redaction-blur-magnify-output.png` |
| Card after Done | Looked at the card | A committed card with Copy, Save and Copy Text (no Edit or Delete) | **The window is still titled "Pending capture" and still advertises "Edit capture" and "Delete pending capture" actions** | **Low–medium (a11y):** state is misannounced. The **"Close thumbnail and keep capture in History" action did nothing** on this card. It only went away later | — |
| Shape and Undo | Rectangle, then Undo | Undo went from enabled to disabled and the rectangle was removed | "Undo" | — | — |
| Dirty close | Red close button on an edited capture | Sheet "Keep this edited capture?" with "Finalize writes the edited image to History. Delete capture discards it. Cancel keeps the editor open." | Buttons "Finalize edited capture", "Delete capture", "Cancel and keep the editor open" | Clear. Cancel kept the editor; Delete Capture discarded (History 10 → 10) | `evidence/p3-dirty-close-sheet.png` |
| Crop | Crop tool, dragged the keep area | Applied immediately (240×140). The preview scales the result up to fill the canvas height | "Drag the area to keep. Everything outside it is removed." | — | — |
| **E. Drag, then cancel** | Dragged the drag well onto a window that accepts no drops | **The capture was committed to History (10 → 11) although nothing was exported. The editor stayed open and editable, and `staging/drag/` kept a 744-byte PNG (240×140, the edited pixels).** After that capture was deleted from History, **the staged PNG was still there** | Drag well: "Drag the edited capture" | **Medium (privacy contract):** cancelled drags finalize and leave pixels outside History's accounting, and they survive Delete | `ls staging/drag` → `02037721-…png 744 B` before and after the delete |
| Close after the drag commit | Red close button | Closed without a prompt | — | Consistent with committed state | — |
