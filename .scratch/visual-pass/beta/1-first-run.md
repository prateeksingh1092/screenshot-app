# Beta person 1: first-run person

Run: 2026-09-24, about 18:45–19:00 local. Installed `~/Applications/Frisket.app` (debug id, build from 73be4ce, PID 47669). All interaction was on the external ASUS display (CG x 1792–3712, scale 1). No keyboard or mouse input from the owner during the run. The "VoiceOver" column is read from AX attributes; VoiceOver itself was not switched on.

Starting state:
- Screen Recording was granted: the status item is `camera.viewfinder`, with AX help "Capture with Frisket".
- The Frisket defaults domain `io.github.prateeksingh1092.frisket.debug` did not exist, so onboarding was never acknowledged and there was no saved shortcut or restore record.
- All seven macOS screenshot hotkeys (28, 29, 30, 31, 181, 182, 184) were already `enabled=False` in `com.apple.symbolichotkeys`.
- History already held 4 captures from 18:29–18:32. They are not from this run and were left alone.

| Surface | What I did | What I saw | What VoiceOver would hear (AX) | What failed or felt unfinished | Evidence |
| --- | --- | --- | --- | --- | --- |
| Status item | Clicked the icon on the external menu bar (x≈3223) | The menu opened under the icon on the external display. Items: Capture Area ⇧⌘4, Capture Window ⇧⌘5, Capture Full Screen ⇧⌘3, Capture Scrolling Page ⇧⌘6, Focus Latest Thumbnail ⇧⌘2, Copy Latest Capture, Delete Latest Capture, History ⇧⌘1, a disabled line "Dismiss captures to keep in History", Settings… ⌘,, About Frisket, Quit Frisket | Status item: "Frisket capture menu", help "Capture with Frisket". Menu items read their titles | Copy Latest Capture and Delete Latest Capture stay enabled with no pending capture, and do nothing silently (`CaptureSurfaces.swift:123-131`). The disabled instruction line reads as a greyed-out item | `/tmp/frisket-beta/menu-crop.png` (not kept in the repo) |
| Onboarding | Tried to open it | There is no menu entry. It appears only at launch while unacknowledged (`LaunchSurfaces.swift:15-37`). It was not on screen, and I was not allowed to relaunch | From code: title "What Frisket stores" as a header. Each statement is read as label plus text, for example "Screen Recording permission, Frisket needs the Screen Recording permission…". Buttons: "Close onboarding and show it again on the next launch" (Later) and "Continue to the Screen Recording permission request" (Continue) | Not reached live. Because the defaults domain is empty, onboarding comes back on every launch until Continue is pressed. The locked phrases are present in `OnboardingContent.swift` | code only |
| About | Menu → About Frisket | A 520×536 dialog centred on the external display. Content: "About Frisket", "Version 0.1.0 (8)", heading "Third-party notices", then a scroll area | Heading "About Frisket". Static text "Version 0.1.0, build 8". The scroll text reads the raw Markdown, starting "# Third-party notices…". Button "Close About" | The notices show raw Markdown: `#`, `##`, `**7.11.1**`, a ```` ```text ```` fence and backticks. "Third-party notices" appears twice (the SwiftUI heading, then `# Third-party notices`). It also shows internal process text to users: "per decision 56", "Imports are confined to `Sources/FrisketCore/StorageAdapter/`", "exact SwiftPM pin" | `evidence/p1-about.png` |
| About close | AXPress "Close About" | Closed. Frisket stayed frontmost with no window | — | — | — |
| Settings open | Menu → Settings… (from the external display's menu) | **The window opened on the main display** at (516,64), 760×852. It is a reused window (id 1341) that kept its old position. I moved it to the external display with AX | Window "Frisket Settings". **Initial keyboard focus is the "Thumbnail auto-dismiss delay in seconds" text field, with "10" selected** | It opens on the other display from the one the menu was used on. The first keystroke overwrites the thumbnail delay, because focus lands in a middle-of-page field instead of the window or the first control | `evidence/p1-settings-top.png` |
| Settings: shortcuts | Read the section | The intro says "⌘⇧4 captures an area, ⌘⇧3 the full screen…". The rows show `⇧⌘1…⇧⌘6`, with "Change" and "Default" per row. There is a "Turn off macOS screenshot shortcuts" button, and no Restore button | Each row reads e.g. "Capture Area shortcut ⇧⌘4". Buttons: "Change Capture Area shortcut", "Restore default Capture Area shortcut", "Turn off the macOS screenshot shortcuts so Frisket can use Command-Shift and a number" | The modifier order differs between the intro (⌘⇧) and the rows (⇧⌘). "Default" is enabled when a row is already default. **"Turn off macOS screenshot shortcuts" always shows (`ShortcutSettings.swift:116`), even though all seven are already off. "Restore" is hidden because the restore record is empty. On this Mac the macOS screenshot shortcuts are off and Frisket cannot turn them back on** | `evidence/p1-settings-top.png`, `/tmp/frisket-beta/symbolichotkeys-before.txt` |
| Settings: record a shortcut | AXPress Change on Capture Scrolling Page, then sent ⌃⌥⌘7 | The button became "Press the new Capture Scrolling Page shortcut", and the row changed to `⌃⌥⌘7`. `globalShortcuts.v1` was written | While recording, the button reads "Press the new Capture Scrolling Page shortcut" | Worked | `evidence/p1-settings-recording.png`, `p1-settings-recorded.png` |
| Settings: put it back | AXPress "Restore default Capture Scrolling Page shortcut" | The row went back to `⇧⌘6`. The defaults now hold an explicit `[captureScrolling, keyCode 22, modifiers 768]` | — | "Default" writes an explicit binding instead of removing the override. Harmless, but the default is now pinned | `defaults export` |
| Settings: lower sections | Scrolled | Thumbnails: Never auto-dismiss switch, Auto-dismiss after (seconds) 10, Apply Thumbnail Settings. History: 30 days, 1,000 MB, Apply History Limits, "History uses 430 KB of 1 GB.". Export folder: full path shown, Choose…. Capture exclusion list: "No apps added.", Add App… | Each control has a clear label, e.g. "History size limit in megabytes", "Add app to capture exclusion list" | Changes need "Apply" buttons, which is unusual for macOS settings. No tabs, so it is one long page. The full path is shown with no Reveal in Finder | `evidence/p1-settings-bottom.png` |
| Permission-missing state | Not reached | I did not revoke the grant. Code check: `refreshPermissionIndicator()` (`FrisketApp.swift:350-373`) swaps to template `exclamationmark.triangle` with label "Frisket capture menu, Screen Recording permission required" and polls every 30 s | — | Not tested live | code only |
