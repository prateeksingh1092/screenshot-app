# Beta person 2: capture person (partial)

Run: 2026-09-24, about 18:50–19:05 local, on the external ASUS display (1920×1080, scale 1), with Frisket 73be4ce (PID 47669). The pattern came from a copy of `Tools/FrisketTestPattern.swift` patched to put its windows on the external display (`/tmp/frisket-beta/pattern`). Input was CGEvents posted to the HID tap, and AX reads and presses.

Two things cut this run short:
- **Around 19:00 the built-in display left the display list.** The ASUS became the only display, at origin (0,0), probably from the lid closing or display sleep; it wasn't caused by the harness. Pointer coordinates after that were off by 1792 until I noticed.
- **The Claude Code safety classifier then blocked further UI driving.** A drag at x < 1792 and an AX focus on Frisket's overlay were both refused. The run stopped there. Window capture, full screen, scrolling, Copy Text and Delete were not reached.

| Surface | What I did | What I saw | What VoiceOver would hear (AX) | What failed or felt unfinished | Evidence |
| --- | --- | --- | --- | --- | --- |
| Area overlay, first look | Pattern frontmost, pointer on the pattern, ⌘⇧4 | Overlay panels at layer 1000 on both displays. A 40% veil: background 42→25 and menu bar 130→78, checked by pixel sampling. The default selection is exactly the pattern, with badge "320 × 180", a white inner stroke and corner ticks. The pattern app stays frontmost (non-activating) | Each overlay is an AXSystemDialog with an AXGroup "Select capture area". Help: "Drag to select. Shift locks an axis. Option grows from the centre. Space moves the selection. Arrow keys nudge one pixel. Shift-arrow resizes one pixel: right and up grow, left and down shrink. Return or keypad Enter captures. Escape cancel…" | The badge and veil are good. The picture stays loudest | `evidence/p2-area-overlay-default.png` |
| Other display's chip | Read-only screenshot of the built-in display, deleted afterwards | Bottom-left solid chip "Selection stays on the display where it started. Esc cancels." | Same group label and help on that display | — | noted only |
| Return on the default selection | Return | Capture 320×180 at scale 1, sRGB. `--verify` PASS. No veil or stroke in the pixels | — | — | History image `331E37D1…` (auto-dismissed) |
| Thumbnail card | Looked at it | The card sits at the bottom-right of the capture display, 288×238. The picture is on top and opaque; the glass controls are below: "Copy" as a word, then Save, Edit and Copy Text as symbols, and a trash button alone on a second row | Window "Pending capture". Custom actions: Copy capture, Save capture, Edit capture, Copy recognized text, Delete pending capture, Close thumbnail and keep capture in History. Buttons: "Copy capture", "Save capture", "Edit capture", "Copy recognized text", "Delete pending capture". The picture itself has no AX element | Delete sits alone on its own row. The picture isn't exposed to VoiceOver, so there's no "image of…" | `evidence/p2-thumbnail.png` |
| Auto-dismiss | Waited more than 10 s | The card closed and the capture was committed to History, as designed | — | Inspecting a card by hand takes longer than 10 s. For this test I turned on "Never auto-dismiss" in Settings, and at the end set the stored value back to false (it applies from the next launch) | — |
| Copy | AXPress "Copy capture" | Clipboard: `public.png` plus `org.nspasteboard.ConcealedType`, 320×180, `--verify` PASS. **The card closes and the capture is finalized into History** | Button goes `enabled=0` while busy | Copy finalizes and closes. So Copy, then Save or Edit, needs History. That's by design (decisions 5 and 11), but CleanShot users won't expect it | `/tmp/frisket-beta/cap2-copy.png` |
| Save | AXPress "Save capture" | Wrote `~/Pictures/Frisket/Frisket-812101E6-1CDB-4B84-BE62-114B40DF3DD6-r1.png` (200×160, sRGB, exact pixels) and the card closed. No sheet, sound or notice | — | **Silent save, and the file name is a UUID** plus `-r1`. There's no date or title to find it by | `ls ~/Pictures/Frisket` |
| Colour | Sampled saved and copied PNGs | The ASUS profile shows blue as violet on screen, but Frisket's PNGs hold exact sRGB (0,0,255), (255,0,0), and background 38 | — | Good | pixel reads |
| Modifiers during a drag | One-process drag starting outside the default hole: Shift, Option, Space, then arrow, Shift-arrow, Shift-up | Badge sequence: "100 × 60", "200 × 60 locked" (height held), "200 × 100", "480 × 260 from centre" (symmetric), "240 × 130 move" (translated), "245 × 135" (resize resumes without a jump), "246 × 136" after →, ⇧→ and ⇧↑. Release captured 246×136 exactly | The badge words are drawn only, not announced | Worked as specified | `evidence/p2-area-modifiers.png`, `p2-area-arrows.png` |
| **Press inside the selection hole** | ⌘⇧4, then drag starting inside the default 320×180 hole. Tested over a titled pattern window's title bar | **The press goes through the overlay to the app underneath. The test window moved by exactly the drag (100,100) while the overlay stayed up.** No selection starts. After that, the overlay group loses focus (`focused=0`), so **Escape goes to the app underneath**: it quit the pattern app, and the veil stayed | Group `focused` 1 → 0 | **High.** A user who starts a new drag inside the preselected area clicks or drags in the real app below (buttons, files, text), and Esc and Return go to that app too. Cause: `SelectionOverlay.swift:33-36` (non-opaque panel, `.clear` background) plus `:152-153` (hole filled with clear via `.copy`), with no `ignoresMouseEvents = false`, so AppKit passes clicks on transparent pixels through. `WindowSelectionOverlay.swift:37-39,151` is built the same way (not tested) | `evidence/p2-clickthrough-before.png`, `p2-clickthrough-after.png`; window bounds 2560,455 → 2660,555 |
| Recovering from a stuck overlay | Clicked the veil outside the hole, then Esc | The overlay took focus back and Esc cancelled | — | You can only recover with the pointer, and nothing tells you so | — |
| **Pointer on the top pixel row** | With the ASUS as the only display, pointer at CG y=0, ⌘⇧4 | Veil everywhere and the "Selection stays on the display where it started. Esc cancels." chip on the **only** display. No default selection. The overlay isn't key (`focused=0`) | Group label unchanged | **Confirms the static review (finding 7): no origin display.** Esc doesn't reach the overlay; it goes to the frontmost app. That's reachable by pushing the pointer to the top edge after using the menu bar. The overlay was left open at the end of the run because further UI driving was blocked | screenshot seen live, not kept |
| Two pending cards | Two captures without dismissing | **The newer card (y 720) sat almost exactly on top of the older one (y 718).** The older card was hidden, with nothing showing a second pending capture. AX said the hidden older card was focused | Two "Pending capture" windows | Expected: newest at the corner, older stacked upward (`CaptureSurfaces.swift:488-505`). Cause not verified | `evidence/p2-thumb-stack.png` |
| ⌘⇧2 Focus Latest Thumbnail | Pattern app frontmost, ⌘⇧2, then Esc | Frisket's AX focused window became the card, but **the frontmost app stayed the pattern app, and Esc went to it (it quit)**. The card stayed. There's no visible focus ring on the card | Card window `focused=1` | **Static finding F confirmed.** Keyboard focus doesn't move to the card, so the card's letter keys, Esc and Delete land in the user's app. There's also no visual focus cue | `evidence/p2-thumb-focused.png` |
| Swipe to dismiss | Synthetic horizontal scroll over the card, with and without precise phased events | No dismissal. The code needs precise phased scroll deltas of 60 pt or more (`ThumbnailPanel.swift:179-193`), which synthetic events didn't produce | — | Not reachable with synthetic input; it needs a real trackpad. Mouse users have no swipe, and Esc/⌘W need the card to be key, which ⌘⇧2 doesn't achieve | — |
| Window capture, full screen, scrolling, Copy Text, Delete, Esc/⌘W dismissal | — | Not reached | — | The run stopped at the classifier block | — |

## Continued in the main session (about 19:50–20:15, any screen allowed)

| Surface | What I did | What I saw | What VoiceOver would hear (AX) | What failed or felt unfinished | Evidence |
| --- | --- | --- | --- | --- | --- |
| **Window capture (⌘⇧5)** | Two titled test windows, pointer over the front one, ⌘⇧5 | **The picker highlights a 28×40 box at the pointer ("28 × 40"), not the window.** ScreenCaptureKit lists the cursor as window id 4 "Cursor", layer 2147483630, whose owner has an **empty-string** bundle ID. `WindowSelection.swift:36` only rejects nil, and there is no level filter, so the cursor is always first under the pointer | Group "Select a window", help "Hover and click, or use the arrow keys or Tab…" | **High: window capture cannot target a window on this Mac** | window-overlay screenshots deleted (they showed desktop files) |
| Window picker click | Clicked the highlighted area | No capture. The overlay stayed up and the click went **through to the app underneath** (the same click-through as the area overlay) | — | High (see the area-overlay row) | — |
| Window picker keyboard | Tab (twice), after clicking the veil to give the overlay focus | The selection did not change. The box stayed where the pointer was when ⌘⇧5 was pressed | — | Tab/arrow selection didn't work live | — |
| Return in the picker | Return | **Alert "Capture unavailable — A disconnected display or an oversized capture can prevent capture. Try again with a smaller area."** | Dialog "alert" | The message blames the display or the size; the real cause is the cursor target | `evidence/p2-window-capture-alert.png` |
| **Full screen (⌘⇧3)** | Full-display pattern on the ASUS, pointer on the ASUS | 1920×1080 capture; `--verify-full … 1920 1080 1` **PASS** (exact sRGB) | — | — | Frisket's own card is not in the pixels |
| Copy Text on an image with no text | Copy Text on a redacted, text-free capture | **Modal alert "Copied 0 characters" with an empty body line, and the clipboard was replaced with an empty string** (`public.utf8-plain-text` 0 B plus Concealed) | Dialog "alert" | **Medium:** a no-text result wipes the user's clipboard and reads as success | `evidence/p2-copytext-zero-alert.png` |
| Card stacking | A new capture while a committed card was showing | **The new card sat on top of the older one again** (y 822 vs 855) | Two "Pending capture" windows | Reconfirms the overlap | `evidence/p2-cards-overlap-2.png` |
| **D. Scrolling: keys** | Page at top. With no session, Page Down scrolls the page. Started ⌘⇧6, then Page Down and Space | **Page Down and Space did nothing to the page while the scrolling panel was up** (the panel takes key on every preview) | Panel "Live scrolling preview", "Scroll the page yourself. Done keeps the image. Cancel discards it.", "Done with scrolling capture", "Cancel scrolling capture" | **Medium: keyboard scrolling is blocked during scrolling capture** | before/after page screenshots |
| **D. Scrolling: steady wheel** | 700×530 region; 4 wheel steps with pauses; Done | 700×1006 image. **Rows silently lost at the seams: block 5 starts 15 rows early, its blue section is 85 rows (expected 90) and its marker is 3 rows (expected 8); block 4 starts 34 rows early and its red section is 77 rows (expected 90).** The page's geometry is fixed (90/90/8, 400-row spacing) | — | **High: scrolling capture silently corrupts content** on a plain synthetic page | `evidence/p2-D-scroll-steady-result.png`, `p2-D-scroll-panel.png` |
| D. Scrolling: fast flick | One 40-line flick after the session started; Done | Not discarded. **The result was 700×416, shorter than the 530-row region selected**: it starts 30 rows lower than the steady run and stops inside block 5. No notice | — | **High: silent truncation** | `evidence/p2-D-scroll-flick-result.png` |
| D. Scrolling: down then up | Two down steps, one up step; Done | Not discarded; 700×1051. Blocks intact, spacing 397 and 399 (expected 400), so 4 rows lost | — | The "any upward scroll discards everything" prediction is **refuted** here | `evidence/p2-D-scroll-up-result.png` |
| Test-tool note | — | `--show-scroll` opens its non-flipped page at the **bottom** ("1 of 6"), so it can only scroll up at first. The page has to be scrolled to the top before a downward session | — | Harness quirk, not Frisket | — |
