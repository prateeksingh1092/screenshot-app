# 13: Thumbnail stack and Delete capture

**What to build:** several captures stack as thumbnails on the capture display, including over full-screen Spaces, and each can be deleted without reaching History or dismissed in every pointer-driven way with a defined outcome.

**Blocked by:** 09

**Status:** ready-for-agent

- [ ] Thumbnails live in a non-activating panel on the capture display and appear over full-screen Spaces.
- [ ] Delete capture discards the Pending capture and writes nothing.
- [ ] Timeout (default delay), swipe, close, overflow, and Esc each finalize to History (decision 44).
- [ ] Each outcome is covered by a seam 1 test through the command layer.
- [ ] Each card's controls have VoiceOver labels.

## Comments
