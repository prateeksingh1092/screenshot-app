{
  "check": "silgen",
  "strict": true,
  "files": {
    "Sources/FrisketCore/StripPNGEncoder.swift": "@_silgen_name(\"crc32\")\nprivate func crc32(_ c: UInt, _ b: UnsafePointer<UInt8>?, _ n: UInt32) -> UInt"
  },
  "expected": [
    "Sources/FrisketCore/StripPNGEncoder.swift: @_silgen_name(\"crc32\") binds a C function with the Swift calling convention"
  ]
}
