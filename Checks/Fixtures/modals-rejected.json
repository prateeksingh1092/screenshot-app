{
  "check": "modals",
  "files": {
    "Frisket/FrisketApp.swift": "private func notice() {\n    let alert = NSAlert()\n    alert.runModal()\n}",
    "Frisket/CaptureSurfaces.swift": "let a = NSAlert.init(error: error)"
  },
  "expected": [
    "Frisket/CaptureSurfaces.swift: NSAlert is only for the destructive or irreversible choices (DA-5); show a Notice instead",
    "Frisket/FrisketApp.swift: NSAlert is only for the destructive or irreversible choices (DA-5); show a Notice instead"
  ]
}
